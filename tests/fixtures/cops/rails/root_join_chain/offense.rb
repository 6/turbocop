Rails.root.join("app").join("models")
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Rails/RootJoinChain: Use a single `join` with multiple arguments instead of chaining.

Rails.root.join("config").join("locales")
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Rails/RootJoinChain: Use a single `join` with multiple arguments instead of chaining.

Rails.public_path.join("assets").join("logo.png")
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Rails/RootJoinChain: Use a single `join` with multiple arguments instead of chaining.