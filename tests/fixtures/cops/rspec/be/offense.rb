it { expect(foo).to be }
                    ^^ RSpec/Be: Don't use `be` without an argument.
it { expect(foo).not_to be }
                        ^^ RSpec/Be: Don't use `be` without an argument.
it { expect(foo).to_not be }
                        ^^ RSpec/Be: Don't use `be` without an argument.
