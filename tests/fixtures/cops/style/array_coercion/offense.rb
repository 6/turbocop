[*paths].each { |path| do_something(path) }
^^^^^^^^ Style/ArrayCoercion: Use `Array(variable)` instead of `[*variable]`.

[*items]
^^^^^^^^ Style/ArrayCoercion: Use `Array(variable)` instead of `[*variable]`.

[*values].map { |v| v.to_s }
^^^^^^^^^ Style/ArrayCoercion: Use `Array(variable)` instead of `[*variable]`.
