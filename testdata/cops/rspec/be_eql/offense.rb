it { expect(foo).to eql(true) }
                    ^^^ RSpec/BeEql: Prefer `be` over `eql`.
it { expect(foo).to eql(false) }
                    ^^^ RSpec/BeEql: Prefer `be` over `eql`.
it { expect(foo).to eql(0) }
                    ^^^ RSpec/BeEql: Prefer `be` over `eql`.
it { expect(foo).to eql(:foo) }
                    ^^^ RSpec/BeEql: Prefer `be` over `eql`.
