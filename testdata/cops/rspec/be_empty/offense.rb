expect(array).to contain_exactly
                 ^^^^^^^^^^^^^^^^ RSpec/BeEmpty: Use `be_empty` matchers for checking an empty array.
expect(array).to match_array([])
                 ^^^^^^^^^^^^^^^ RSpec/BeEmpty: Use `be_empty` matchers for checking an empty array.
expect(list).to contain_exactly
                ^^^^^^^^^^^^^^^^ RSpec/BeEmpty: Use `be_empty` matchers for checking an empty array.
