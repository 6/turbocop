Date.today
^^^^^^^^^^ Rails/Date: Use `Date.current` instead of `Date.today`.

x = Date.today
    ^^^^^^^^^^ Rails/Date: Use `Date.current` instead of `Date.today`.

if Date.today > deadline
   ^^^^^^^^^^ Rails/Date: Use `Date.current` instead of `Date.today`.
end
