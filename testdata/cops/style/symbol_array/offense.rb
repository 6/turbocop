[:foo, :bar, :baz]
^ Style/SymbolArray: Use `%i` or `%I` for an array of symbols.

[:one, :two]
^ Style/SymbolArray: Use `%i` or `%I` for an array of symbols.

x = [:alpha, :beta, :gamma, :delta]
    ^ Style/SymbolArray: Use `%i` or `%I` for an array of symbols.
