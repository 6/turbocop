if a and b
     ^^^ Style/AndOr: Use `&&` instead of `and`.
  do_something
end

if a or b
     ^^ Style/AndOr: Use `||` instead of `or`.
  do_something
end

while x and y
        ^^^ Style/AndOr: Use `&&` instead of `and`.
  do_something
end
