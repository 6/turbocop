-> { nil }
^^^^^^^^^^ Style/NilLambda: Use an empty lambda instead of always returning nil.

proc { nil }
^^^^^^^^^^^^ Style/NilLambda: Use an empty proc instead of always returning nil.

Proc.new { nil }
^^^^^^^^^^^^^^^^ Style/NilLambda: Use an empty proc instead of always returning nil.

lambda { nil }
^^^^^^^^^^^^^^ Style/NilLambda: Use an empty lambda instead of always returning nil.
